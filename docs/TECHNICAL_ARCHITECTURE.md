# R³ 개인별 맞춤 다이어리 - 기술 아키텍처

## 📐 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        설문 입력 계층                              │
│  ┌──────────────┬──────────────┬──────────────┐               │
│  │ Google Forms │  n8n Web Form│  Web UI      │               │
│  └──────────────┴──────────────┴──────────────┘               │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                     n8n-MCP (Webhook)                            │
│  • 설문 응답 수집                                               │
│  • 데이터 검증                                                 │
│  • 이벤트 발행                                                 │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                   FastAPI Backend                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Data Processing Pipeline                                │  │
│  │  • 설문 데이터 정규화                                    │  │
│  │  • 프로필 객체 생성                                      │  │
│  │  • 데이터베이스 저장                                     │  │
│  └─────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Personalization Engine                                   │  │
│  │  • 리듬 분석 (사주/기문)                                  │  │
│  │  • 성향 기반 콘텐츠 생성                                  │  │
│  │  • 역할 기반 번역                                        │  │
│  └─────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Content Quality Validation                              │  │
│  │  • 글자 수 검증                                         │  │
│  │  • 의미 일관성 확인                                     │  │
│  │  • 개인화 레벨 점수 계산                                │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│            Customer Segmentation & Routing                       │
│  ┌─────────────┬─────────────┬──────────────┐                 │
│  │ App Only    │ Hybrid      │ Paper Only   │                 │
│  └─────────────┴─────────────┴──────────────┘                 │
└─────────────────────────────────────────────────────────────────┘
          ↓                    ↓                    ↓
    ┌──────────┐      ┌────────────────┐      ┌──────────┐
    │ Web App  │      │ PDF Generator  │      │ Printer  │
    │+ Mobile  │      │ + Email        │      │  Handler │
    │  Render  │      │  Service       │      │          │
    └──────────┘      └────────────────┘      └──────────┘
          ↓                    ↓                    ↓
    ┌──────────┐      ┌────────────────┐      ┌──────────┐
    │ Frontend │      │ Mailgun/       │      │ Print    │
    │ (Next.js)│      │ SendGrid +     │      │ Provider │
    │          │      │ AWS S3         │      │          │
    └──────────┘      └────────────────┘      └──────────┘
```

## 🗂️ 디렉토리 구조 (추가)

```
diary-PJ/
├── backend/
│   ├── src/
│   │   ├── api/
│   │   │   ├── forms/              ← 설문 API
│   │   │   ├── profiles/           ← 프로필 API
│   │   │   ├── personalization/    ← 개인화 API
│   │   │   ├── content/            ← 콘텐츠 API
│   │   │   ├── customers/          ← 고객 관리 API
│   │   │   ├── segmentation/       ← 세분화 API
│   │   │   └── analytics/          ← 분석 API
│   │   │
│   │   ├── services/
│   │   │   ├── form_collector.py   ← 설문 수집
│   │   │   ├── data_processor.py   ← 데이터 정규화
│   │   │   ├── personalizer.py     ← 개인화 엔진
│   │   │   ├── pdf_generator.py    ← PDF 생성
│   │   │   ├── email_service.py    ← 이메일 발송
│   │   │   ├── segmenter.py        ← 고객 세분화
│   │   │   └── router.py           ← 콘텐츠 라우팅
│   │   │
│   │   ├── integrations/
│   │   │   ├── n8n_handler.py      ← n8n 연동
│   │   │   ├── google_forms.py     ← Google Forms
│   │   │   ├── mailgun_handler.py  ← Mailgun 연동
│   │   │   └── s3_handler.py       ← AWS S3 연동
│   │   │
│   │   └── models/
│   │       ├── form_models.py
│   │       ├── profile_models.py
│   │       ├── customer_models.py
│   │       └── content_models.py
│   │
│   ├── tests/
│   │   ├── test_forms/
│   │   ├── test_profiles/
│   │   ├── test_personalization/
│   │   ├── test_segmentation/
│   │   └── test_integration/
│   │
│   └── mcps/
│       ├── n8n_mcp/               ← n8n MCP 서버
│       ├── email_service_mcp/     ← 이메일 MCP
│       ├── file_storage_mcp/      ← 파일 저장소 MCP
│       └── google_forms_mcp/      ← Google Forms MCP
│
├── frontend/
│   ├── src/
│   │   ├── app/
│   │   │   ├── (auth)/            ← 인증 페이지
│   │   │   ├── profile/           ← 프로필 페이지
│   │   │   ├── forms/             ← 설문 페이지
│   │   │   ├── today/             ← 일간 페이지
│   │   │   ├── month/             ← 월간 페이지
│   │   │   ├── year/              ← 연간 페이지
│   │   │   ├── dashboard/         ← 대시보드
│   │   │   └── settings/          ← 설정 페이지
│   │   │
│   │   ├── components/
│   │   │   ├── forms/             ← 설문 폼 컴포넌트
│   │   │   ├── content/           ← 콘텐츠 표시 컴포넌트
│   │   │   ├── analytics/         ← 분석 차트
│   │   │   └── common/            ← 공통 컴포넌트
│   │   │
│   │   └── lib/
│   │       ├── api.ts
│   │       ├── segmentation.ts    ← 고객 타입 판별
│   │       └── personalization.ts ← 개인화 로직
│   │
│   └── skills/
│       └── ... (claude code skills)
│
├── pdf-generator/
│   ├── templates/
│   │   ├── monthly_diary.html
│   │   ├── yearly_plan.html
│   │   ├── daily_page.html
│   │   └── cover.html
│   │
│   ├── styles/
│   │   ├── print.css
│   │   └── typography.css
│   │
│   └── generator.py
│
├── docs/
│   ├── DEVELOPMENT_ROADMAP.md     ← 개발 로드맵
│   ├── TECHNICAL_ARCHITECTURE.md  ← 기술 아키텍처
│   ├── IMPLEMENTATION_CHECKLIST.md ← 구현 체크리스트
│   ├── content/
│   ├── legal/
│   └── ...
│
└── claude-code-skills/
    ├── form-builder/
    ├── personalization-engine/
    ├── pdf-layout-optimizer/
    ├── email-template-designer/
    ├── customer-segmentation/
    ├── data-validator/
    └── content-quality-checker/
```

## 🔌 MCP 서버 명세

### 1. n8n-mcp
```
목적: n8n 워크플로우 자동화
구현 언어: Python/TypeScript
제공 도구:
  • create_workflow() - 워크플로우 생성
  • execute_workflow() - 즉시 실행
  • list_workflows() - 워크플로우 목록
  • get_workflow_results() - 실행 결과 조회
```

### 2. file-storage-mcp
```
목적: S3/Azure Blob 파일 관리
구현 언어: Python
제공 도구:
  • upload_file() - 파일 업로드
  • download_file() - 파일 다운로드
  • delete_file() - 파일 삭제
  • generate_signed_url() - 임시 다운로드 URL
```

### 3. email-service-mcp
```
목적: 이메일 발송 및 템플릿 관리
구현 언어: Python
제공 도구:
  • send_email() - 단일 이메일 발송
  • send_batch() - 대량 이메일 발송
  • get_template() - 템플릿 조회
  • save_template() - 템플릿 저장
```

### 4. google-forms-mcp
```
목적: Google Forms API 연동
구현 언어: Python
제공 도구:
  • create_form() - 폼 생성
  • get_responses() - 응답 수집
  • update_form() - 폼 수정
  • watch_responses() - 실시간 감시
```

## 🎨 Claude Code Skill 명세

### 1. form-builder
```
목적: 설문 폼 자동 설계
입력: 수집할 정보 목록
출력: 폼 JSON / n8n 설정
기능:
  • 필드 타입 선택
  • 조건부 로직
  • 검증 규칙
```

### 2. personalization-engine
```
목적: 프로필 → 개인화 콘텐츠
입력: 고객 프로필
출력: 맞춤형 콘텐츠
기능:
  • 리듬 분석
  • 성향 매핑
  • 콘텐츠 생성
```

### 3. pdf-layout-optimizer
```
목적: 인쇄용 PDF 최적화
입력: 콘텐츠 + 템플릿
출력: 최적화된 PDF
기능:
  • 레이아웃 설계
  • 페이지 분할
  • 폰트 임베딩
```

### 4. email-template-designer
```
목적: HTML 이메일 템플릿 설계
입력: 이메일 요구사항
출력: HTML 템플릿
기능:
  • 반응형 디자인
  • 개인화 변수
  • 클라이언트 호환성
```

### 5. customer-segmentation
```
목적: 고객 분류 및 라우팅
입력: 고객 프로필 + 구독 정보
출력: 배포 채널
기능:
  • 세그먼트 분류
  • 라우팅 규칙
  • 예외 처리
```

### 6. data-validator
```
목적: 데이터 검증
입력: 설문 응답
출력: 검증 결과
기능:
  • 필드 타입 검증
  • 범위 검증
  • 중복 검사
```

### 7. content-quality-checker
```
목적: 콘텐츠 품질 검증
입력: 생성된 콘텐츠
출력: 품질 점수
기능:
  • 글자 수 검증
  • 의미 일관성
  • 개인화 레벨
```

## 💾 데이터베이스 설계

### Customers 테이블
```sql
CREATE TABLE customers (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    email VARCHAR(255) UNIQUE NOT NULL,
    name VARCHAR(255) NOT NULL,
    birth_date DATE,
    profile_data JSONB,
    subscription_type VARCHAR(50), -- 'app_only', 'hybrid', 'paper_only'
    preferences JSONB,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    deleted_at TIMESTAMP
);
```

### Customer Profiles 테이블
```sql
CREATE TABLE customer_profiles (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    customer_id UUID REFERENCES customers(id),
    personality_traits JSONB,
    interests JSONB,
    preferences JSONB,
    lifecycle_stage VARCHAR(50),
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### Generated Content 테이블
```sql
CREATE TABLE generated_content (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    customer_id UUID REFERENCES customers(id),
    content_date DATE,
    content JSONB,
    quality_score FLOAT,
    web_url VARCHAR(500),
    pdf_path VARCHAR(500),
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### Subscriptions 테이블
```sql
CREATE TABLE subscriptions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    customer_id UUID REFERENCES customers(id),
    subscription_type VARCHAR(50),
    status VARCHAR(50), -- 'active', 'paused', 'cancelled'
    start_date DATE,
    end_date DATE,
    billing_cycle VARCHAR(50),
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### Distribution History 테이블
```sql
CREATE TABLE distribution_history (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    customer_id UUID REFERENCES customers(id),
    content_date DATE,
    channel VARCHAR(50), -- 'web', 'email', 'print'
    status VARCHAR(50), -- 'pending', 'sent', 'failed'
    sent_at TIMESTAMP,
    tracking_info JSONB
);
```

## 🔄 데이터 흐름

### 설문 수집 → 프로필 생성
```
[설문 응답]
    ↓
[n8n 수집 & 검증]
    ↓
[데이터 정규화]
    ↓
[프로필 객체 생성]
    ↓
[데이터베이스 저장]
    ↓
[프로필 ID 반환]
```

### 프로필 → 콘텐츠 생성
```
[고객 프로필]
    ↓
[리듬 분석 (사주/기문)]
    ↓
[성향 기반 매핑]
    ↓
[콘텐츠 생성 (AI)]
    ↓
[역할 기반 번역]
    ↓
[품질 검증]
    ↓
[데이터베이스 저장]
    ↓
[배포 준비]
```

### 콘텐츠 → 배포
```
[생성된 콘텐츠]
    ↓
[고객 세분화]
    ├─ [앱 전용] → [웹 렌더링]
    ├─ [하이브리드] → [웹 + PDF + 이메일]
    └─ [종이 전용] → [PDF + 인쇄소 연동]
    ↓
[배포 실행]
    ↓
[추적 및 로깅]
    ↓
[분석 데이터 저장]
```

## 🔐 보안 고려사항

### 인증 & 인가
- JWT 토큰 기반 인증
- Role-based Access Control (RBAC)
- 고객 데이터 격리

### 데이터 보호
- 민감한 데이터 암호화 (생년월일 등)
- GDPR 준수 (삭제 기능 구현)
- 감사 로그 유지

### API 보안
- Rate Limiting
- Input Validation
- CORS 설정
- API Key 관리

## 📊 성능 최적화

### 캐싱 전략
- Redis 캐시 (콘텐츠 템플릿)
- CDN (정적 자산)
- 데이터베이스 쿼리 결과 캐시

### 데이터베이스 최적화
- 인덱싱 (customer_id, content_date)
- 파티셔닝 (날짜 기반)
- 쿼리 최적화

### 비동기 처리
- Celery 작업 큐
- 대량 PDF 생성 병렬 처리
- 이메일 배치 발송

## 📈 모니터링 & 로깅

### 주요 지표
- API 응답 시간
- 콘텐츠 생성 시간
- 배포 성공률
- 고객 만족도

### 로깅 전략
- 구조화된 로깅 (JSON)
- 로그 집계 (ELK Stack)
- 실시간 알림 (Slack)

## 🚀 배포 전략

### 개발 환경
- localhost:5000 (Frontend)
- localhost:8000 (Backend)
- localhost:5432 (PostgreSQL)

### 스테이징 환경
- AWS / Azure
- 완전한 환경 복제
- 성능 테스트

### 프로덕션 환경
- 고가용성 구성
- 자동 백업
- 재해 복구 계획
